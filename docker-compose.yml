version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_APP=backend/app.py
      - FLASK_ENV=development
    env_file:
      - .env
    volumes:
      - .:/app
    command: python backend/app.py
